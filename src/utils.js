export const headingIDTOF = () => {
  return `root-bab-${parseInt(Math.random() * 1000)}`;
};

export const subHeadingIDTOF = (current) => {
  return `${current}-subbab-${parseInt(Math.random() * 1000)}`;
};


